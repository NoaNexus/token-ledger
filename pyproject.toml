[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "token-ledger-local"
version = "2.1.1"
description = "Local-first Windows token usage dashboard for Codex, Claude Code, and Antigravity"
readme = "README.md"
requires-python = ">=3.11"
license = { file = "LICENSE" }
authors = [{ name = "Token Ledger contributors" }]
dependencies = ["PySide6-Essentials==6.8.3"]
classifiers = [
  "Development Status :: 4 - Beta",
  "Environment :: Win32 (MS Windows)",
  "License :: OSI Approved :: MIT License",
  "Operating System :: Microsoft :: Windows",
  "Programming Language :: Python :: 3.11",
  "Topic :: System :: Monitoring",
]

[project.scripts]
token-ledger = "tokenledger.native:main"

[tool.setuptools.packages.find]
include = ["tokenledger*"]

[tool.pytest.ini_options]
testpaths = ["tests"]
