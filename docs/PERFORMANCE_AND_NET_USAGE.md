# 性能与净用量

桌面端使用环回 HTTP 服务、PyQt5 和 Qt WebEngine。扫描增量索引，周期任务约每 60 秒触发，不重叠执行。窗口尺寸受可用工作区限制。界面支持减少动画偏好，不承诺固定帧率。

仪表盘缓存按数据库及 WAL 文件状态、内部写版本、时区和短期时间桶失效；共享缓存更新串行化，避免并发请求互相污染。额度随时间老化，不等待下一次日志写入才能过期。

净用量 = max(Input - Cached input, 0) + Output。它只描述扣除缓存读取的流量，不是实付量；缓存写入包含在 Input 中，计费估算按独立写入参考价处理。
